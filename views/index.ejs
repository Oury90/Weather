<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title>WeatherData</title>
</head>
<body>
    <div class="container">
        <% if (locals.data_temp){ %>
            <% let temp_c = Math.floor(parseInt(data_temp) - 273.15) %>

            <h2><span><%= temp_c %></span>&#8451;</h2>
            <div class="specifique">
                <p>main: <strong> <%= description1 %> </strong></p>
                <p>humidity: <strong> <%= description3 %></strong></p>
                <p>Description: <strong> <%= description2 %> </strong></p>
            </div>
            <div class="image">
                <img src=<%= icon %> alt="Weather icon">
            </div>
        <% } %>
        
    </div>
</body>
</html>